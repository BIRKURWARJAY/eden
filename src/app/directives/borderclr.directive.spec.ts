import { BorderclrDirective } from './borderclr.directive';

describe('BorderclrDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderclrDirective();
    expect(directive).toBeTruthy();
  });
});
